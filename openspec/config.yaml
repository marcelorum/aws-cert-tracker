# openspec/config.yaml
schema: spec-driven

context: |
  Tech stack: Greenfield — no stack detected yet. Anticipated: JavaScript/TypeScript (Node.js) for an AWS CLF certification tracking web application.
  Architecture: Not yet defined.
  Testing: No test runner detected. No package.json, no test config files exist.
  Style: Not yet defined.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner: null
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: null
    type_checker: null
    formatter: null
